# Rules Index - wiki-backend-code-review

> This index provides a compact summary of all rule files. The SKILL.md feature scanner uses these summaries to determine which rule files to load. Rule files themselves contain the full rule details.

## Quick Reference

| Rule File | Rules | Critical | Suggestion | Trigger Keywords |
|-----------|-------|----------|------------|------------------|
| sse-streaming-rule.md | 6 | 5 | 1 | text/event-stream, reply.raw.write, reply.raw.writeHead |
| filesystem-vault-rule.md | 6 | 5 | 1 | fs/promises, index.md, log.md, FileStateStore, vault/ |
| route-design-rule.md | 5 | 4 | 1 | app.post, app.get, request.body, Content-Type, Fastify route |
| harness-integration-rule.md | 6 | 6 | 0 | EngineAdapter, AsyncIterable, yield, beforeLoop, afterLoop |
| security-rule.md | 5 | 5 | 0 | apiKey, path.join, execFile, spawn, app.listen, 0.0.0.0 |
| error-handling-rule.md | 5 | 5 | 0 | try/catch, instanceof Error, error event, state file corruption |
| config-management-rule.md | 5 | 3 | 2 | reset-config, restore, updateConfig, preset lists, test-connection |
| data-consistency-rule.md | 21 | 0 | 0 | display, brand, normalize, task_links, enrichment |
| session-state-rule.md | 18 | 0 | 0 | invalidate_cache, cookie, session, health check, cross-process |
| persistence-cache-rule.md | 6 | 4 | 2 | import.meta.url, process.cwd(), refreshConfigCache, request.params.id, path.join, localStorage, IndexedDB, data/conversations, .gitignore |
| encoding-safety-rule.md | 4 | 4 | 0 | UTF-8, BOM, GB2312, tsc, encoding, Set-Content, Out-File, WriteAllText, 中文注释 |
| cleanup-audit-rule.md | 6 | 4 | 2 | cleanup, audit, JSONL, dry_run, days, loadStatus, refreshConfigCache, deleteOne, errors[], batch delete |
| route-registration-backend-rule.md | 1 | 1 | 0 | routes/*.ts, index.ts, register, app.post, app.get, Fastify route, 404 |
| null-guard-rule.md | 1 | 1 | 0 | this.provider, this.child, this.connection, spawn, exec, connect, listen, TS2531, possibly null |
| graceful-shutdown-rule.md | 1 | 1 | 0 | spawn, setInterval, connect, listen, SIGINT, SIGTERM, process.on, process.exit, child.kill, clearInterval |
| sensitive-field-masking-rule.md | 1 | 1 | 0 | apiKey, authtoken, password, secret, token, GET /config, mask, configured, empty string |
| type-sync-backend-rule.md | 1 | 1 | 0 | types.ts, export interface, frontend, backend, sync, ConversationMeta, monorepo, import type |
| config-merge-preservation-rule.md | 1 | 1 | 0 | config.json, writeFile, readFile, merge, shallow, preserve_sections, tunnel, llm, server |
| update-check-backend-rule.md | 4 | 3 | 1 | check-update, has_update, offline_mode, cache_ttl, GitHub API, releases/latest, AbortController, current_version |
| architecture-rule.md | 4 | 4 | 0 | routes, workflows, engine, vault, 分层, 依赖注入, 跨层调用 |
| config-consistency-rule.md | 4 | 4 | 0 | getEffectiveApiKey, loadConfig, process.env, 配置读取, 统一函数 |
| hot-update-rule.md | 3 | 3 | 0 | updateConfig, saveAiConfig, 热更新, 热重载, 闭环 |
| esm-and-wiring-rule.md | 4 | 4 | 0 | __dirname, __filename, import.meta.url, ESM, 接线, registerRoute, SonarQube, S6606, 404, ReferenceError |
| async-reliability-rule.md | 4 | 3 | 1 | asyncio.wait_for, Promise.race, 超时, 心跳, threading.Thread, worker_threads, best_holder, 兜底 |
| external-api-contract-rule.md | 4 | 3 | 1 | fetch(, fetchWithDiagnostics, err.cause.code, UND_ERR_CONNECT_TIMEOUT, ENOTFOUND, ECONNREFUSED, data.url, data.metadata.url, String(value), ProxyAgent |
| timeout-tier-rule.md | 5 | 3 | 2 | AbortSignal.timeout, setTimeout, task_creation, task_polling, image_generation, video_download, llm_small, llm_large, proxy_overhead_ms, tokenBudget |
| api-key-resolution-rule.md | 4 | 3 | 1 | apiKey, apiKeyRef, apiKeys, dedicated, shared, env, fallback_chain, resolveApiKey, getEffectiveApiKey, process.env[, not configured |
| long-task-architecture-rule.md | 6 | 4 | 2 | SSE, text/event-stream, rateLimit, timeWindow, destructive, poll, setInterval, request.log.error, reply.code, not configured, 设计要点 |
| media-archive-frontmatter-rule.md | 5 | 3 | 2 | frontmatter, type: query, output_mode, generated_at, image_file, video_file, vault/queries/, writeFileSync, marp: true, filename_pattern |
| session-store-rule.md | 5 | 4 | 1 | new Map(, Map.set, Map.delete, Map.keys().next(), sessions, max_sessions, ttl_ms, expiresAt, sessionId, messageIndex, trust_client_content |
| sse-event-dispatch-rule.md | 5 | 4 | 1 | text/event-stream, reply.raw.write, event:, chunk.text, chunk.image, if/else if, Record<string, Handler>, outputMode, outputModes, S3776 |
| powershell-long-process-rule.md | 4 | 2 | 2 | EPIPE, broken pipe, exit code -1, vue-tsc, orchestrator.py, powershell pipe, long-running process, Select-String, Out-String, Where-Object |
| rollback-minimal-rule.md | 4 | 2 | 2 | rollback, restore, revert, partial rollback, recovery order, Edit tool, Write forbidden, deleted code, functional recovery |
| rate-limit-resilience-rule.md | 4 | 3 | 1 | X-Forwarded-For, trustProxy, rateLimit, sliding window, sweep, Map, constant-time, timingSafeEqual, clock-independent, 60s boundary, dual-dimension, username, IP+username |
| deterministic-text-processing-rule.md | 5 | 5 | 0 | regex, unicode, CJK, char class, /[㐀-鿿　-〿＀-￯]/, codepoint, ideograph, tokenizer, dedup, Jaccard, coherence, compression budget, recency window |
| runtime-data-privacy-rule.md | 2 | 2 | 0 | .gitignore, runtime data dir, data/, gitignore sync, sessions, server-side, privacy leak |
| config-portable-defaults-rule.md | 2 | 2 | 0 | dataDir, stateDir, userData, vaultPath, portable defaults, relative path, machine-absolute, SEA, multi-machine, first persist |
| path-traversal-strict-rule.md | 2 | 2 | 0 | path.join, sanitize, whitelist, .., isAbsolute, 路径穿越, 二次校验 |
| packaging-userdata-rule.md | 4 | 4 | 0 | installer, ignoreversion, {app}, Program Files, IS_SEA, getUserDataDir, getDataDir, LOCALAPPDATA, machine-absolute, rebase, clean defaults, user data dir |
| spa-live-deploy-rule.md | 4 | 4 | 0 | public_live_, resolveSpaRoot, spaRoot, isDeployComplete, .deploy-complete, /wiki/, within-root, path.relative, 时间戳目录, 重启后端 |
| ../wiki-code-dev/references/indexeddb-test-isolation-rule.md | 3 | 3 | 0 | indexedDB, localStorage, deleteDatabase, setTimeout(0), vi.clearAllMocks, vi.resetModules, 命名空间, 唯一 userId, fake-indexeddb, onblocked, 异步落盘 |
| server-side-isolation-rule.md | 3 | 3 | 0 | preHandler, requireAdmin, requireAuth, createIsolationGuards, ownerId, owner_id, X-Forwarded-For, clientIpFromRequest, request.ip, 401, 403, admin, RBAC, auth.enabled, trustProxy, 限流复合键, 服务端盖章 |

## Detailed Index

### sse-streaming
- File: sse-streaming-rule.md
- Rules: 6 (5 critical, 1 suggestion)
- Core checks: headers completeness, event format, finally cleanup, error event pushing, no reply.send, unified send helper
- Scope: Fastify routes returning text/event-stream, reply.raw.write, SSE helper functions

### filesystem-vault
- File: filesystem-vault-rule.md
- Rules: 6 (5 critical, 1 suggestion)
- Core checks: writable whitelist, path traversal prevention, concurrent lock, partial failure strategy, async API, temp files
- Scope: Obsidian Vault read/write, fs/promises calls, index.md/log.md appends, FileStateStore, temp file handling

### route-design
- File: route-design-rule.md
- Rules: 5 (4 critical, 1 suggestion)
- Core checks: parameter validation, Content-Type dispatching, unified registration signature, GET=JSON/POST=SSE, SRP in handlers
- Scope: Fastify route registration, request parsing, handler implementations

### harness-integration
- File: harness-integration-rule.md
- Rules: 6 (6 critical)
- Core checks: complete EngineAdapter implementation, AsyncIterable yield, budget_exceeded handling, beforeLoop/afterLoop hooks, FileStateStore persistence, prompt file loading
- Scope: engine/ adapter implementations, workflows/ hook registration, state/ FileStateStore

### security
- File: security-rule.md
- Rules: 5 (5 critical)
- Core checks: API key via env only, path validation regex, command whitelist, localhost binding, error info sanitization
- Scope: All api/ code involving external services, path concatenation, child processes, network binding

### error-handling
- File: error-handling-rule.md
- Rules: 5 (5 critical)
- Core checks: try-catch + user-friendly message, instanceof Error guard, SSE error event (not throw), tool failure in loop, state file graceful degradation
- Scope: All try/catch logic, error response construction, SSE error events, state file read/write

### config-management
- File: config-management-rule.md
- Rules: 5 (3 critical, 2 suggestions)
- Core checks: reset-default endpoint, runtime adapter sync, field-level reset only, centralized presets, full lifecycle (GET/PUT/POST reset/POST test)
- Scope: Config route handlers, config persistence logic, adapter runtime sync, preset constants

### data-consistency
- File: data-consistency-rule.md
- Rules: 21 (all best practices)
- Core checks: brand-title validation, stale display cleanup, detail page overwrite, enrichment normalization, no fallback to stale values, cross-table consistency
- Scope: Display field normalization, brand inference, cross-table data enrichment

### session-state
- File: session-state-rule.md
- Rules: 18 (all best practices)
- Core checks: cache invalidation before reads, cross-process propagation, 3-tier session check, runtime cookie injection, layer state sync, health check staleness
- Scope: Shared state file reads, cross-process state propagation, cache invalidation, cookie/authentication state

### persistence-cache
- File: persistence-cache-rule.md
- Rules: 6 (4 critical, 2 suggestions)
- Core checks: import.meta.url path anchor (no CWD-only), write-then-refresh cache, UUID/whitelist regex for HTTP params, backend-as-authority-source for cross-origin data, fallback warn logging, .gitignore sync for new disk dirs
- Scope: Config file path resolution, in-memory cache refresh after write, HTTP route params to filesystem mapping, cross-origin persistence boundary, fallback degradation logging, disk directory gitignore sync

### encoding-safety
- File: encoding-safety-rule.md
- Rules: 4 (4 critical)
- Core checks: source files UTF-8 no BOM, config files UTF-8 no BOM, strict UTF-8 decode with U+FFFD indicator, PowerShell preserves encoding when editing Chinese comments
- Scope: .ts/.json/.md file encoding, Edit-after write detection, pre-tsc compile gate, PowerShell maintenance scripts editing Chinese-commented files

### cleanup-audit
- File: cleanup-audit-rule.md
- Rules: 6 (4 critical, 2 suggestions)
- Core checks: JSONL audit log with non-blocking failure, complete audit fields (timestamp/target/days/dry_run/cleaned_count/errors), days lower-bound protection, dry_run default true, refresh state after execute, per-item independent try-catch with errors[] collection
- Scope: Cleanup/archive/recycle routes in api/src/routes/, batch delete workflows, audit log writers, days/dry_run parsing, post-cleanup state refresh

### route-registration-backend
- File: route-registration-backend-rule.md
- Rules: 1 (1 critical)
- Core checks: every non-exempt routes/*.ts file is imported and register() called in entry file (index.ts); route directory scan vs entry-file registration call diff; exempt files explicitly listed
- Scope: Manual route registration frameworks (Fastify/Express/Koa), entry file route imports, routes/ directory vs entry-file registration consistency

### null-guard
- File: null-guard-rule.md
- Rules: 1 (1 critical)
- Core checks: fields assigned via spawn/exec/connect/listen must be `T | null`, used only after `if (!x)` guard; guard branch has observable recovery (rebuild/throw/log); rebuild path has retry cap
- Scope: Class members holding external async-assigned resources (child_process, db connection, file handle, long-lived client), TS2531 / TS18047 compile errors

### graceful-shutdown
- File: graceful-shutdown-rule.md
- Rules: 1 (1 critical)
- Core checks: modules creating spawn/setInterval/connect/listen register SIGINT/SIGTERM hooks; cleanup order is child_process → timer → connection; cleanup ends with process.exit(0); single-resource cleanup failure does not block others
- Scope: Long-lived services with child processes, polling timers, long-lived connections; container rolling update / process exit scenarios

### sensitive-field-masking
- File: sensitive-field-masking-rule.md
- Rules: 1 (1 critical)
- Core checks: GET handler masks fields matching key/token/secret/password/authtoken patterns with last4_padstart; returns `<field>_configured: boolean` flag; POST/PUT treats empty string as no-change (skip update)
- Scope: Config GET/POST/PUT handlers for sensitive fields (apiKey/authtoken/password/secret), serialization helpers, frontend config display

### type-sync-backend
- File: type-sync-backend-rule.md
- Rules: 1 (1 critical)
- Core checks: backend types.ts `export interface` (or sync_interfaces list) exists in frontend types.ts; field names, type signatures, optionality match; monorepo `import type` exemption
- Scope: Full-stack TypeScript projects (backend Node + frontend React/Vue), API contract types defined in types.ts, PRs touching types.ts on either side

### config-merge-preservation
- File: config-merge-preservation-rule.md
- Rules: 1 (1 critical)
- Core checks: writeFile on config_file_path reads original first; merges per merge_strategy (shallow/deep); preserve_sections retained with original values; multi-module shared config uses unified mergeConfigSection() helper
- Scope: Multi-section config files (config.json with tunnel/llm/server/engine sections), module-level save handlers (PUT /api/{module}/config), exempt paths for tmp/cache files

### update-check-backend
- File: update-check-backend-rule.md
- Rules: 4 (3 critical, 1 suggestion)
- Core checks: module-level memory cache with TTL (default 5 min); offline_mode returns fixed {has_update:false, source:'local'}; response contains 6 required fields (has_update/current_version/latest_version/release_url/source/checked_at); external GitHub API call uses AbortController timeout (default 10s) and degrades to source:'local' on failure
- Scope: /api/about/check-update route handlers, GitHub Releases API integration, version comparison logic, offline mode fallback

### architecture
- File: architecture-rule.md
- Rules: 4 (4 critical)
- Core checks: layer order (routes→workflows→engine→vault), no cross-layer calls (routes→vault forbidden), single responsibility per layer, dependency injection via constructor, minimal module exports
- Scope: Code changes touching routes/workflows/engine/vault layer boundaries

### config-consistency
- File: config-consistency-rule.md
- Rules: 4 (4 critical)
- Core checks: all config read entries use unified functions (getEffectiveApiKey/loadConfig), no direct process.env reads for config, new config fields follow full lifecycle (types→config→index→routes GET/PUT), backward compatibility with optional fields
- Scope: Code involving config.json reads, environment variable access, hot reload config paths

### hot-update
- File: hot-update-rule.md
- Rules: 3 (3 critical)
- Core checks: updateConfig covers all hot-updatable fields, PUT handler calls updateConfig after save, hot update verifiable via GET endpoint, save→persist→updateConfig→response chain
- Scope: Code involving updateConfig, saveAiConfig, hot-updatable config fields

### esm-and-wiring
- File: esm-and-wiring-rule.md
- Rules: 4 (4 critical)
- Core checks: BR-ESM-01 __dirname/__filename forbidden in ESM, BR-ESM-02 new routes/workflows modules must complete 3-step wiring (import→instantiate→registerRoute), BR-ESM-03 static analysis suggestions require manual runtime verification, BR-ESM-04 route endpoints reachable from entry file
- Scope: ESM migration, new route/workflow modules, SonarQube/ESLint suggestion adoption, route registration

### async-reliability
- File: async-reliability-rule.md
- Rules: 4 (3 critical, 1 warning)
- Core checks: BR-ASYNC-01 all await calls wrapped with timeout (Promise.race/asyncio.wait_for), BR-ASYNC-02 event-loop-blocking scenarios use thread-level heartbeat (threading.Thread/worker_threads), BR-ASYNC-03 status file ts updates independent of business event loop, BR-ASYNC-04 timeout fallback provides best_holder data instead of raising
- Scope: Async code with IPC/network/file I/O/external process calls, Playwright API usage, state file communication patterns

- spa-static-hosting-rule.md — SPA 静态资源托管规则（SH-1 ~ SH-5）

## BR-026 ~ BR-029 新增条目

> v1.3.0 追加的 4 条新规则索引，格式：规则编号 | 触发关键字 | 核心检查 | 规则文件路径

| 规则编号 | 触发关键字 | 核心检查 | 规则文件路径 |
|---------|-----------|---------|-------------|
| BR-026 | reply.send, reply.raw.write, return result, response shape, API contract, types.ts, export interface | API 端点响应形状与前端 types.ts interface 同步；新增字段前后端同步 | references/api-response-type-sync-rule.md |
| BR-027 | text/event-stream, reply.raw.write, event: xxx, SSE event types, consumeSSEStream, sse_event_types | 新增 SSE 事件类型须同步 config 列表与前端 consumeSSEStream handler；未知事件禁止静默吞掉 | references/sse-event-type-route-rule.md |
| BR-028 | updateConfig, merge, optional, ??, !, non-null assertion, as, mergeConfigSection, Partial<T> | 合并可选配置字段禁止用 `!` 断言或 `as` 强转，必须用 `??` 默认值或 if 守卫 | references/optional-merge-rule.md |
| BR-029 | EngineAdapter, interface, implements, beforeLoop, afterLoop, yield, compile, query, as unknown as | 接口新增/修改方法须同步所有实现类；禁止 `as unknown as` / `@ts-ignore` 绕过 implements 检查 | references/engine-adapter-sync-rule.md |

### api-response-type-sync（BR-026）
- File: api-response-type-sync-rule.md
- Rules: 2 (2 critical)
- Core checks: reply.send 响应形状与前端 types.ts interface 字段名/类型/可选性一致；新增响应字段前后端同一 PR 同步；monorepo `import type` 豁免
- Scope: 后端 routes/*.ts 中 reply.send 调用、前端 types.ts interface 定义、API 契约同步

### sse-event-type-route（BR-027）
- File: sse-event-type-route-rule.md
- Rules: 2 (2 critical)
- Core checks: 后端 SSE 事件类型须登记在 config 列表；前端 consumeSSEStream 须同步 handler 分支；未知事件策略符合 config（warn/error/ignore）
- Scope: 后端 SSE 路由（text/event-stream）、config 事件类型列表、前端 SSE 消费函数

### optional-merge（BR-028）
- File: optional-merge-rule.md
- Rules: 2 (2 suggestion)
- Core checks: 配置合并函数中访问可选字段禁止用 `!` 后缀断言；禁止用 `as T` 强转；必须用 `??` 或 if 守卫处理 undefined
- Scope: 配置合并函数（updateConfig/mergeConfig/mergeConfigSection）、Partial<T> 字段访问、TS 类型断言绕过

### engine-adapter-sync（BR-029）
- File: engine-adapter-sync-rule.md
- Rules: 2 (2 critical)
- Core checks: EngineAdapter 接口方法须在所有 implements 类中同步实现且签名一致；禁止 `as unknown as` / `@ts-ignore` 绕过检查；可选方法用 `method?()` 声明
- Scope: api/src/engine/adapter.ts 接口定义、所有 implements EngineAdapter 实现类、workflow 调用方存在性检查

## BR-034 ~ BR-037 新增条目

> v1.5.0 追加的 4 条新规则索引，格式：规则编号 | 触发关键字 | 核心检查 | 规则文件路径

| 规则编号 | 触发关键字 | 核心检查 | 规则文件路径 |
|---------|-----------|---------|-------------|
| BR-034 | process.env., app.listen(, path.join(, new Set([, apiKey, port, path, timeout, secret | 硬编码可配置参数检测：API Key/port/path/timeout/secret 必须通过 config.json 或环境变量注入 | references/config-persistence-boundary-rule.md |
| BR-035 | import.meta.url, process.cwd(), getConfigPath, getStateDir, fileURLToPath | 路径解析锚点合规性：禁止 process.cwd() 作为唯一锚点，必须三级策略 | references/config-persistence-boundary-rule.md |
| BR-036 | refreshConfigCache, saveAiConfig, resetAiConfig, saveWebSearchConfig, writeFile | 缓存刷新机制：写盘后必须立即刷新内存缓存，TTL 不替代显式刷新 | references/config-persistence-boundary-rule.md |
| BR-037 | localStorage, IndexedDB, data/conversations, apiKey, config.json, CRUD route | 跨 origin 持久化边界：后端为权威源，浏览器存储仅作降级缓存 | references/config-persistence-boundary-rule.md |

### config-persistence-boundary（BR-034~037）
- File: config-persistence-boundary-rule.md
- Rules: 4 (4 critical)
- Core checks: 硬编码可配置参数检测（process.env./app.listen/path.join/new Set）、路径解析三级策略（import.meta.url + 打包兜底 + CWD 探测）、写盘后立即刷新缓存、跨 origin 数据后端权威源+localStorage 降级缓存
- Scope: api/ 下涉及配置持久化、文件落盘、内存缓存、HTTP 路由参数到文件系统映射的代码；与 persistence-cache-rule.md 的 PC-1/PC-2 互为补充

## BR-038 ~ BR-047 新增条目

> v1.6.0 追加的 10 条新规则索引（基于 Tauri 2.x 桌面应用集成历史问题复盘），格式：规则编号 | 触发关键字 | 核心检查 | 规则文件路径

| 规则编号 | 触发关键字 | 核心检查 | 规则文件路径 |
|---------|-----------|---------|-------------|
| BR-038 | tauri-build-debug.ps1, tauri-build-release.ps1, pnpm --filter, pnpm run build, SPA 构建步骤 | Tauri 构建脚本必须包含 SPA 构建步骤且用 `pnpm --filter <pkg> build`（禁止 `pnpm run build` 递归触发 tauri） | references/tauri-build-script-rule.md |
| BR-039 | Remove-Item, services/api/public/, Test-Path, index.html, emptyOutDir | 构建前清理后端 public 旧产物；构建后验证 index.html 存在性（缺失则中断构建） | references/tauri-build-script-rule.md |
| BR-040 | PSDrive, .Free, disk space, 磁盘空间, cargo cache, debug_min_gb, release_min_gb | debug/release 构建前磁盘空间预检查（debug>=3GB, release>=5GB，阈值从 config 读取） | references/tauri-build-script-rule.md |
| BR-041 | build.rs, AppManifest, commands, #[tauri::command], invoke_handler, generate_handler | build.rs 须显式声明 AppManifest::commands 注册所有自定义命令；与源码 #[tauri::command] 函数列表对比 | references/tauri-capability-config-rule.md |
| BR-042 | capabilities/default.json, permissions, allow-, core:default, capability, ACL | capabilities/default.json 的 permissions 须包含所有 allow-<command> 权限与必备核心权限 | references/tauri-capability-config-rule.md |
| BR-043 | remote, urls, external-urls, webview, OAuth, 外部 URL | 外部 URL 加载须配置 remote.urls 嵌套结构（非顶层 urls）+ core:webview:allow-external-urls 权限 | references/tauri-capability-config-rule.md |
| BR-044 | tauri.conf.json, app.security.capabilities, default, identifier | tauri.conf.json 的 app.security.capabilities 须引用 default，且引用的 capability 文件实际存在 | references/tauri-capability-config-rule.md |
| BR-045 | Start-Process, -NoNewWindow, -Wait, -PassThru, & cargo, & pnpm, & node | PowerShell 调用外部工具须用 Start-Process 包装，禁止直接 & 调用（stderr 会触发 Stop 终止） | references/powershell-stderr-rule.md |
| BR-046 | 2>&1, Out-Host, Out-File, Tee-Object, RedirectStandardError | 禁止用 `2>&1 \| Out-Host` 合并 stderr 与 stdout（错误语义丢失），须用 RedirectStandardError 单独重定向 | references/powershell-stderr-rule.md |
| BR-047 | $ErrorActionPreference, Stop, ErrorRecord, $Error, LASTEXITCODE | Stop 模式下所有外部工具调用必须用 Start-Process 避免 stderr 误报；建议统一 Stop + Start-Process 模式 | references/powershell-stderr-rule.md |

### tauri-build-script（BR-038~040）
- File: tauri-build-script-rule.md
- Rules: 3 (2 critical, 1 suggestion)
- Core checks: SPA 构建步骤必须存在且用 `pnpm --filter <pkg> build`（避免递归触发 tauri）、构建前清理 services/api/public/ 旧产物 + 构建后 Test-Path 验证 index.html、磁盘空间预检查（debug>=3GB, release>=5GB）
- Scope: Tauri 2.x 桌面应用 PowerShell 构建脚本（tauri-build-debug.ps1 / tauri-build-release.ps1）、CI/CD 构建配置

### tauri-capability-config（BR-041~044）
- File: tauri-capability-config-rule.md
- Rules: 4 (4 critical)
- Core checks: build.rs AppManifest::commands 注册所有 #[tauri::command]、capabilities/default.json permissions 含 allow-<command>、remote.urls 嵌套格式（非顶层 urls）+ external-urls 权限、tauri.conf.json app.security.capabilities 引用 default
- Scope: Tauri 2.x src-tauri/ 下的 build.rs、capabilities/*.json、tauri.conf.json、src/ 下 #[tauri::command] 标注的函数

### powershell-stderr（BR-045~047）
- File: powershell-stderr-rule.md
- Rules: 3 (2 critical, 1 suggestion)
- Core checks: 调用 cargo/rustc/pnpm/node 等用 Start-Process -NoNewWindow -Wait -PassThru（非 & 直接调用）、禁止 2>&1 | Out-Host 合并流（须 RedirectStandardError 单独重定向）、Stop 模式下所有外部工具调用必须用 Start-Process
- Scope: 所有 .ps1 PowerShell 脚本中调用外部可执行文件（cargo/rustc/pnpm/npm/node/tsc/tsx/git/go/python/java/dotnet）的代码段；Tauri 构建脚本、CI/CD PowerShell 步骤

## 认证端点分类（BR-048~049）
- 文件：references/auth-endpoint-classification-rule.md
- 触发关键词：publicPaths, preHandler, authGuard, /api/auth/me, currentUser, 401, token 解析, setupAuthMiddleware, isPublicPath
- 核心检查：publicPaths 只含完全公开端点（login/health），需鉴权端点（/api/auth/me）禁止放入；preHandler 跳过 token 解析会导致 currentUser 永远 null

## Windows 文件操作（BR-050~051）
- 文件：references/windows-file-operation-rule.md
- 触发关键词：fs.rm, rmSync, rd /s /q, execSync, existsSync, Windows, 文件句柄, 静默失败, 路径穿越, SKILL_ID_PATTERN
- 核心检查：win32 平台目录删除用 rd /s /q + existsSync 验证；fs.rm 作为 fallback；删除后必须验证结果；路径穿越防护在删除前执行

## 多资源并行加载（BR-052）
- 文件：references/parallel-loading-backend-rule.md
- 触发关键词：Promise.allSettled, Promise.all, for await, for...of, 串行加载, 并行加载, MCP, loadMcpTools, loadResources, fetchMultiple, errors[], source, 降级, independent resources, allSettled vs all, 资源加载, 工具加载, partial failure
- 核心检查：N 个独立资源加载必须用 Promise.allSettled 并行（禁止 for await 串行）；必须用 allSettled 而非 all（单失败不阻断）；rejected 必须收集到 errors 数组含 source 字段；资源内部子任务串行（并行粒度聚焦资源层级）；必须有总超时兜底防止永久挂起

## 超时阈值链式匹配（BR-053）
- 文件：references/timeout-chain-backend-rule.md
- 触发关键词：timeout, setTimeout, AbortController, 超时, 阈值, chain, layer, mcpTimeoutMs, questionTimeoutMs, frontend timeout, backend timeout, mcp timeout, llm timeout, 调用链, 链式超时, margin, 余量, 递增覆盖, partial result, SSE partial, degradation, 降级路径, 硬编码超时
- 核心检查：多层调用链超时必须自下而上递增覆盖（每层 ≥ 下层 × margin_multiplier）；超时阈值必须从 config 读取禁止硬编码；超时中断必须保留已收到的部分结果（禁止清空 SSE 部分答案）；超时后必须有降级路径（如跳过 MCP 用内置工具）；阈值变更必须同步检查链上所有层

## BR-054 ~ BR-060 新增条目

> v1.9.0 追加的 7 条新规则索引（基于 v3 媒体生成工具开发复盘提炼），对应 wiki-code-dev 的 CODING-056~063 编码规范。
> 格式：规则编号 | 触发关键字 | 核心检查 | 规则文件路径

| 规则编号 | 触发关键字 | 核心检查 | 规则文件路径 |
|---------|-----------|---------|-------------|
| BR-054 | fetch(, fetchWithDiagnostics, err.cause.code, UND_ERR_CONNECT_TIMEOUT, ENOTFOUND, ECONNREFUSED, data.url, data.metadata.url, String(value), Go 后端, type conversion, field fallback, 双重兼容, undici, ProxyAgent | 业务代码调用外部 API 必须用 fetchWithDiagnostics 包装 + err.cause.code 错误码分类翻译 + 响应字段双重路径兼容 + Go/Rust 后端类型显式转换 + export 供单元测试 | references/external-api-contract-rule.md |
| BR-055 | AbortSignal.timeout, setTimeout, task_creation, task_polling, image_generation, video_download, llm_small, llm_large, proxy_overhead_ms, tokenBudget, 超时, 分级, 字面量, 硬编码超时 | 调用外部 API 必须按预估耗时分级设置超时（6 个分级）+ 超时阈值从 config 读取禁止硬编码 + 调用类型与分级语义匹配 + LLM 按 tokenBudget 分级 + 代理场景加宽 | references/timeout-tier-rule.md |
| BR-056 | apiKey, apiKeyRef, apiKeys, dedicated, shared, env, fallback_chain, resolveApiKey, getEffectiveApiKey, process.env[, config.llm.apiKeys, config.media, 三级回退, 密钥解析, not configured | 密钥解析必须按专用段 → 共享段 → 环境变量三级回退 + 回退顺序固定禁止调整 + 错误消息列出三个配置位置 + 统一 null 语义 | references/api-key-resolution-rule.md |
| BR-057 | SSE, text/event-stream, rateLimit, timeWindow, destructive, poll, setInterval, request.log.error, reply.code, 400, 500, not configured, 设计要点, 路由注释, 长任务, 异步任务 | 分钟级任务走独立 JSON 端点 + 前端轮询 + 破坏性端点限流 + 错误码 400/500 区分 + 双日志通道 + 路由顶部设计要点注释 + 轮询限流匹配 | references/long-task-architecture-rule.md |
| BR-058 | frontmatter, type: query, output_mode, generated_at, image_file, video_file, task_id, source_url, vault/queries/, archive, writeFileSync, writeFile, marp: true, 双重 frontmatter, 文件名, YYYYMMDD, ISO8601 | 归档目录在 WRITE_ALLOWED_DIRS 白名单 + frontmatter 含 type/output_mode/generated_at 必备字段 + 文件名匹配正则禁止 UUID + 业务字段同 frontmatter + Marp 合并 frontmatter | references/media-archive-frontmatter-rule.md |
| BR-059 | new Map(, Map.set, Map.delete, Map.keys().next(), sessions, session, max_sessions, ttl_ms, expiresAt, evictExpired, LRU, TTL, sessionId, messageIndex, trust_client_content, content, answer, result, 防篡改, 引用模式, 内存溢出 | 内存 Map 双层淘汰（TTL + LRU）+ 读取检查 TTL + 引用模式禁止接收客户端内容 + trust_client_content 防篡改 + 写入记录 expiresAt | references/session-store-rule.md |
| BR-060 | text/event-stream, reply.raw.write, event:, chunk.text, chunk.image, chunk.ppt, chunk.video, if/else if, else, Record<string, Handler>, handlers, outputMode, outputModes, 联合字面量, S3776, cognitive complexity, 未知事件, console.warn, default handler | 后端独立 if 分发禁止 if/else if/else 链 + 前端事件类型 ≥3 用对象映射表 + outputMode 联合字面量类型 + outputMode/outputModes 正交分离 + 未知事件 default handler | references/sse-event-dispatch-rule.md |

### external-api-contract（BR-054）
- File: external-api-contract-rule.md
- Rules: 4 (3 critical, 1 suggestion)
- Core checks: 业务代码调用外部 API 必须用 fetchWithDiagnostics 包装原生 fetch（按 err.cause.code 分类翻译错误码）；响应字段双重路径兼容（data.url || data.metadata?.url）；Go/Rust 后端字段类型显式转换（String/Number/Boolean）；fetchWithDiagnostics 必须 export 供单元测试
- Scope: api/src/ 下调用第三方/外部 API 的业务代码（media-generation-workflow.ts 等）、fetchWithDiagnostics 实现体、外部 API 响应消费代码
- 对应编码规范：CODING-056

### timeout-tier（BR-055）
- File: timeout-tier-rule.md
- Rules: 5 (3 critical, 2 suggestion)
- Core checks: 6 个超时分级（task_creation 30s / task_polling 30s / image_generation 60s / video_download 120s / llm_small 60s / llm_large 180s）；超时阈值从 config 读取禁止字面量硬编码；调用类型与分级语义匹配；LLM 按 tokenBudget 分级（4k → llm_small，16k → llm_large）；代理场景加宽 proxy_overhead_ms
- Scope: api/src/ 下所有 AbortSignal.timeout 和 setTimeout 调用、LLM 调用超时配置、代理场景外部 API 调用
- 对应编码规范：CODING-057

### api-key-resolution（BR-056）
- File: api-key-resolution-rule.md
- Rules: 4 (3 critical, 1 suggestion)
- Core checks: 密钥解析按专用段 → 共享段 → 环境变量三级回退（fallback_chain 配置）；回退顺序固定禁止调整（专用段优先级最高）；错误消息列出三个配置位置（error_message_template）；每级回退返回 null 进入下一级（统一 null 语义）
- Scope: api/src/ 下所有需要 API Key 的外部服务调用（Agnes / OpenAI / Anthropic 等）、resolveApiKey/getEffectiveApiKey 函数实现、密钥缺失错误处理
- 对应编码规范：CODING-058

### long-task-architecture（BR-057）
- File: long-task-architecture-rule.md
- Rules: 6 (4 critical, 2 suggestion)
- Core checks: 分钟级任务（> sse_threshold_ms 30s）走独立 JSON 端点 + 前端轮询禁止走 SSE；破坏性端点限流 ≤ 5/min；错误码区分配置缺失（400）与 API 失败（500）；错误双日志通道（request.log.error + reply.send）；路由顶部"设计要点"块注释；轮询端点限流与前端 poll_interval_ms 匹配
- Scope: api/src/routes/ 下长任务路由（视频生成/批量处理/文件转码等）、rateLimit 配置、错误处理 catch 块、路由文件顶部注释
- 对应编码规范：CODING-059

### media-archive-frontmatter（BR-058）
- File: media-archive-frontmatter-rule.md
- Rules: 5 (3 critical, 2 suggestion)
- Core checks: 归档目录在 WRITE_ALLOWED_DIRS 白名单（默认 vault/queries/）；frontmatter 含 type/output_mode/generated_at 必备字段；文件名匹配 filename_pattern 正则（禁止随机 UUID）；业务字段放 frontmatter 禁止散落正文；Marp 类归档合并 frontmatter（禁止双重）
- Scope: api/src/workflows/ 下 LLM 生成产物归档代码（media-generation-workflow.ts 等）、writeFileSync/writeFile 调用、frontmatter 字符串构造、Marp 类归档文件
- 对应编码规范：CODING-060

### session-store（BR-059）
- File: session-store-rule.md
- Rules: 5 (4 critical, 1 suggestion)
- Core checks: 内存 Map 双层淘汰（TTL + LRU，max_sessions 100）；读取检查 expiresAt 过期返回 null；归档/查询接口用引用模式（sessionId + messageIndex）禁止接收 content/answer/result；trust_client_content=false 时客户端内容字段必须被忽略；写入记录 expiresAt 禁止无 TTL 写入
- Scope: api/src/services/ 下会话存储类（SessionStore 等）、new Map 会话存储、归档/查询路由请求体解构、sessionStore.get/set 方法
- 对应编码规范：CODING-061

### sse-event-dispatch（BR-060）
- File: sse-event-dispatch-rule.md
- Rules: 5 (4 critical, 1 suggestion)
- Core checks: 后端 SSE 按字段独立 if 分发禁止 if/else if/else 链；前端事件类型 ≥3 用 Record<string, Handler> 对象映射表（S3776 < 15）；outputMode 联合字面量类型禁止 string 泛型；outputMode（结构模式）与 outputModes（事件可见性）正交分离；未知事件 default handler 禁止静默吞掉
- Scope: api/src/routes/ 下 SSE 路由（text/event-stream / reply.raw.write）、前端 SSE 事件处理器（consumeSSEStream 等）、outputMode 类型声明、对象映射表查找代码
- 对应编码规范：CODING-063

## BR-061 ~ BR-062 新增条目

> v1.10.0 追加的 2 条新规则索引（基于「顶部导航栏→左侧侧栏 + 编码规范提炼」五维度复盘），对应 CODING-059~060。
> 格式：规则编号 | 触发关键字 | 核心检查 | 规则文件路径

| 规则编号 | 触发关键字 | 核心检查 | 规则文件路径 |
|---------|-----------|---------|-------------|
| BR-061 | EPIPE, broken pipe, exit code -1, vue-tsc, orchestrator.py, powershell pipe, long-running process, Select-String, Out-String, Where-Object, tsconfig.tsbuildinfo, node_modules/.vite | 长时进程（≥30s）禁止通过 PowerShell 管道运行；退出码 -1 视为 EPIPE 而非真实失败；vue-tsc 超时先清增量缓存；需过滤输出重定向到文件 | —（对应 wiki-code-dev references/powershell-long-process-rule.md） |
| BR-062 | rollback, restore, revert, partial rollback, recovery order, Edit tool, Write forbidden, deleted code, functional recovery | 恢复已删除代码按删除反向顺序用 Edit 精准替换禁止 Write 重写整文件；回滚后双验(类型检查+E2E)；部分回滚需注释标注 | —（对应 wiki-code-dev references/rollback-minimal-rule.md） |

### powershell-long-process（BR-061）
- File: —（共同引用 wiki-code-dev references/powershell-long-process-rule.md）
- Rules: 4 (2 critical, 2 suggestion)
- Core checks: PowerShell 长时进程禁止管道（PLP-1/2/3/4）；vue-tsc/orchestrator.py 等直接运行；退出码 -1 → EPIPE 断裂（非真实失败），重新直接运行验证；vue-tsc > 2min 清理增量缓存（node_modules/.tmp + tsconfig.tsbuildinfo + node_modules/.vite + src/**/*.js）
- Scope: 所有 PowerShell 构建/测试/类型检查脚本（tauri-build-*.ps1 / orchestrator.py / vue-tsc/tsc 调用）
- 对应编码规范：CODING-059

### rollback-minimal（BR-062）
- File: —（共同引用 wiki-code-dev references/rollback-minimal-rule.md）
- Rules: 4 (2 critical, 2 suggestion)
- Core checks: 回滚按删除反向顺序（import→emit→template→style→parent-binding）；用 Edit 精准替换（禁 Write 重写整文件）；回滚后双验（vue-tsc/tsc + E2E 测试）；部分回滚用注释标注保留删除部分
- Scope: 所有代码恢复/回滚操作（git revert 后手动恢复 / A/B 测试回滚 / 需求变更恢复）
- 对应编码规范：CODING-060

### rate-limit-resilience（BR-063）
- File: rate-limit-resilience-rule.md
- Rules: 4 (3 critical, 1 suggestion)
- Core checks: 反向代理下从 X-Forwarded-For 还原真实客户端 IP（trustProxy off）；滑动/有界窗口 + 定时 sweep（无无界 Map）；令牌校验常量时间（timingSafeEqual）；限流测试时钟无关避免 60s 边界 flake
- Scope: api/src/ 下限流中间件、IP 提取、令牌校验、限流单测；SEA/多机反向代理部署
- 对应编码规范：CODING-RATE-LIMIT-RESILIENCE

### deterministic-text-processing（BR-064）
- File: deterministic-text-processing-rule.md
- Rules: 5 (5 critical)
- Core checks: CJK/Unicode 正则仅表意文字区间（排除全角标点）；去重实体感知（Jaccard≥阈值 且 无显著数字/[[ref]] 差异）；低价值=空/纯标点/问候黑名单（禁仅按短）；压缩预算 max(floor(len*0.3), minFloor) 保证净下降；驱逐保护连贯性地板 min(recencyWindow, K)
- Scope: api/src/ 下分词/去重/摘要/上下文压缩/记忆驱逐的确定性文本处理代码；token 估算
- 对应编码规范：CODING-DETERMINISTIC-TEXT

### runtime-data-privacy（BR-065）
- File: runtime-data-privacy-rule.md
- Rules: 2 (2 critical)
- Core checks: 每个新增运行时落盘目录须与兄弟目录一并 gitignore；持久化默认保守（会话默认不服务端存储，仅显式开启）
- Scope: api/src/ 下新增落盘目录、会话存储、配置文件写入；扩展 BR-030~037 持久化/目录分离
- 对应编码规范：CODING-RUNTIME-DATA-PRIVACY

### config-portable-defaults（BR-066）
- File: config-portable-defaults-rule.md
- Rules: 2 (2 critical)
- Core checks: dataDir/stateDir 从稳定用户数据目录推导（独立于 vaultPath）；首次持久化写可移植默认（相对/可重派生），禁止提交机器绝对路径
- Scope: api/src/ 下 getStateDir/getDataDir、config.json 首次写入、SEA/多机部署配置
- 对应编码规范：CODING-CONFIG-PORTABLE

### path-traversal-strict（BR-067）
- File: path-traversal-strict-rule.md
- Rules: 2 (2 critical)
- Core checks: 白名单 sanitize 后二次负向匹配 `/(^|\/)\.\.(\/|$)/`；拼接前 `path.isAbsolute` 二次检查；扩展 security-rule 路径穿越防护
- Scope: api/src/ 下所有 `path.join(vaultRoot, userInput)` 路径拼接；vault/文件读写入口
- 对应编码规范：（扩展 security-rule / input-validation-rule，无新增 wiki-code-dev 规则）

### packaging-userdata（BR-068）
- File: —（共同引用 wiki-code-dev references/packaging-userdata-rule.md）
- Rules: 4 (3 critical, 1 suggestion)
- Core checks: 安装器 `[Files]` 仅含程序文件且用 `ignoreversion`，用户数据绝不打包到 {app}（防覆盖+防 Program Files 无写权限）；资源路径（getApiDir/getResourcePath）与用户数据路径（getUserDataDir/getDataDir）经不同函数派生，`IS_SEA` 统一分支；首次落盘回退机器绝对路径为相对默认（rebase 仅对相对路径，尊重用户显式绝对路径）；用户数据目录首次运行 `mkdirSync(recursive)` 自建
- Scope: 安装器脚本（*.iss / *.ps1）、runtime.ts 路径解析、config.ts SEA rebase 与首次落盘；SEA/多机部署 config.json 持久化
- 对应编码规范：CODING-PACKAGING-USERDATA

### spa-live-deploy（BR-071）
- File: spa-live-deploy-rule.md
- Rules: 4 (4 critical)
- Core checks: 托管根目录从候选（含 `public_live_<ts>` 时间戳目录）选数值时间戳最大且通过完整性门禁（index.html + `.deploy-complete`）；禁止取"第一个存在 index.html 的候选"（扩展 SH-1）；`/wiki/*` 资源须 `path.normalize` + `path.relative(spaRoot, abs)` 逃逸拦截（与 BR-067 同纵深）；部署写全新时间戳目录不覆盖已存在目录（规避 safe-delete 钩子）、写完须重启后端
- Scope: api/src/index.ts 的 spaRoot 解析、入口文件静态资源托管、部署脚本（_deploy_live.mjs 等）、`/wiki/*` 资源定位逻辑
- 对应编码规范：CODING-SPA-LIVE-DEPLOY

## BR-063 ~ BR-067 新增条目

> v2.0.0 追加的 5 条新规则索引（基于 SEA/多机部署、确定性文本处理、隐私与路径安全复盘提炼），对应 wiki-code-dev 的 CODING-RATE-LIMIT-RESILIENCE / CODING-DETERMINISTIC-TEXT / CODING-RUNTIME-DATA-PRIVACY / CODING-CONFIG-PORTABLE（BR-067 扩展 security-rule，无新增编码规范）。
> 格式：规则编号 | 触发关键字 | 核心检查 | 规则文件路径

| 规则编号 | 触发关键字 | 核心检查 | 规则文件路径 |
|---------|-----------|---------|-------------|
| BR-063 | X-Forwarded-For, trustProxy, rateLimit, sliding window, sweep, Map, constant-time, timingSafeEqual, clock-independent, 60s boundary, fixed window, dual-dimension, username, IP+username | 反向代理还原真实客户端 IP（trustProxy off）；已鉴权端点限流键组合「真实 IP + 用户名」双维度；滑动/有界窗口 + sweep（无无界 Map）；令牌常量时间校验；限流测试时钟无关 | references/rate-limit-resilience-rule.md |
| BR-064 | regex, unicode, CJK, /[㐀-鿿　-〿＀-￯]/, codepoint, ideograph, tokenizer, dedup, Jaccard, coherence, compression budget, recency window | CJK 正则仅表意文字（排除全角标点）；去重实体感知（Jaccard+数字/[[ref]] 差异）；低价值=空/纯标点/问候（禁仅按短）；压缩预算净下降；驱逐连贯性地板 | references/deterministic-text-processing-rule.md |
| BR-065 | .gitignore, runtime data dir, data/, gitignore sync, sessions, server-side, privacy leak | 新增运行时落盘目录须 gitignore（与兄弟同组）；持久化默认保守（会话默认不服务端存） | references/runtime-data-privacy-rule.md |
| BR-066 | dataDir, stateDir, userData, vaultPath, portable defaults, relative path, machine-absolute, SEA, multi-machine, first persist | dataDir/stateDir 从稳定用户数据目录推导（独立于 vaultPath）；首次持久化写可移植默认（禁机器绝对路径） | references/config-portable-defaults-rule.md |
| BR-067 | path.join, sanitize, whitelist, .., isAbsolute, 路径穿越, 二次校验 | 白名单 sanitize 后二次匹配 `/(^|\/)\.\.(\/|$)/`；拼接前 `path.isAbsolute` 二次检查（扩展 security-rule） | references/path-traversal-strict-rule.md |

## BR-068 新增条目

> v2.1.0 追加的 1 条新规则索引（基于「安装器覆盖配置 / AppData 数据迁移 / SEA 路径解析 / clean-defaults」复盘，对应 wiki-code-dev 的 CODING-PACKAGING-USERDATA）。
> 格式：规则编号 | 触发关键字 | 核心检查 | 规则文件路径

| 规则编号 | 触发关键字 | 核心检查 | 规则文件路径 |
|---------|-----------|---------|-------------|
| BR-068 | installer.iss, ignoreversion, {app}, Program Files, IS_SEA, getUserDataDir, getDataDir, LOCALAPPDATA, APPDATA, os.homedir, rebase, clean defaults, machine-absolute, user data dir, getApiDir | 安装器程序文件 ignoreversion、用户数据绝不打包到 {app}；资源路径与用户数据路径经不同函数派生；IS_SEA 统一分支；首次落盘回退机器绝对路径为相对默认；用户数据目录首次运行自建 | references/packaging-userdata-rule.md（wiki-code-dev） |

## BR-069 ~ BR-070 新增条目

> v2.2.0 追加的 2 条新规则索引（基于「raw 文件名修复 + 走查建议优化」复盘，对应 wiki-code-dev 的 CODING-USER-UPLOAD-FILENAME / CODING-MIGRATION-SAFETY）。
> 格式：规则编号 | 触发关键字 | 核心检查 | 规则文件路径

| 规则编号 | 触发关键字 | 核心检查 | 规则文件路径 |
|---------|-----------|---------|-------------|
| BR-069 | archiveRaw, sanitize, basename, 中文, CJK, 汉字, 全角, 文件名, 上传, wiki-batch, wiki-compile, stripInternalPrefix, internal prefix, originalName | Unicode 感知清洗（`/[^\p{L}\p{N}._-]/gu`，中文不被变下划线）+ 内部前缀剥离（wiki-batch-/wiki-compile-/input-）+ 清洗后 `..`/`isAbsolute` 二次校验（与 BR-067 一致）+ originalName? 透传 `??` 兜底 | references/user-upload-filename-rule.md（wiki-code-dev） |
| BR-070 | migrate, migration, 迁移, 重写, recover, source:, replace, 批量重命名, recover-title, dry-run, --apply, 脏数据, pageCache | dry-run 默认 + 幂等可恢复（冲突后缀 `-2/-3`）+ 函数式 `$` 替换 `(_, g1) => g1 + newName` + pageCache 两阶段（先扫描后改写） | references/data-repair-script-safety-rule.md（wiki-code-dev） |

## BR-071 新增条目

> v2.3.0 追加的 1 条新规则索引（基于「/wiki/* 路径穿越加固 + 实时部署解析」复盘，对应 wiki-code-dev 的 CODING-SPA-LIVE-DEPLOY，扩展 spa-static-hosting-rule.md SH-1~SH-5）。
> 格式：规则编号 | 触发关键字 | 核心检查 | 规则文件路径

| 规则编号 | 触发关键字 | 核心检查 | 规则文件路径 |
|---------|-----------|---------|-------------|
| BR-071 | public_live_, resolveSpaRoot, spaRoot, isDeployComplete, .deploy-complete, /wiki/, within-root, path.relative, 时间戳目录, 重启后端, safe-delete | 托管根目录从候选（含 `public_live_<ts>`）选数值时间戳最大且完整性门禁通过（index.html + `.deploy-complete`，扩展 SH-1）；`/wiki/*` 资源 normalize + within-root 逃逸拦截（与 BR-067 同纵深）；部署写全新时间戳目录不覆盖已存在（规避 safe-delete 钩子）、写完须重启后端 | references/spa-live-deploy-rule.md（wiki-code-dev） |

## BR-074 ~ BR-080 新增条目

> v2.6.0 追加的 7 条新规则索引（基于「SSML 注入 / 子进程异步同步 / 关键写吞错 / 文件损坏 / as any 绕过 / 超时硬编码 / 冗余探针」后端编码规范复盘提炼），对应 wiki-code-dev 的 CODING-SSML-INJECTION / CODING-CHILD-PROCESS-SYNC / CODING-CRITICAL-WRITE-NO-SWALLOW / CODING-FILE-CORRUPTION-GUARD / CODING-TYPE-SAFE-NO-ANY / CODING-CONFIG-TIMEOUT / CODING-NO-REDUNDANT-PROBE。
> 格式：规则编号 | 触发关键字 | 核心检查 | 规则文件路径

| 规则编号 | 触发关键字 | 核心检查 | 规则文件路径 |
|---------|-----------|---------|-------------|
| BR-074 | prosody, rate, volume, pitch, SSML, express-as, escapeXml, /api/tts/synthesize, 1007, mstts | prosody rate/volume/pitch 白名单正则校验 + 用户文本 `<>&` 转义 + 禁用免费端点不支持的 express-as（WebSocket 1007）；前后端双重防护（前端 FR-073 先校验） | references/ssml-injection-rule.md（wiki-code-dev） |
| BR-075 | execFile, execFileSync, spawn, child_process, ffmpeg, await, callback, timeout | 需同步结果用 execFileSync（带超时）或 await Promise，禁异步 execFile 当同步用；缺失工具优雅降级 | references/child-process-sync-rule.md（wiki-code-dev） |
| BR-076 | saveUsers, saveConfig, persistConversation, writeFile, try/catch, ok: true | 关键写失败必须 throw / log+throw，禁空 catch 假成功；返回 ok 须与真实写一致 | references/critical-write-no-swallow-rule.md（wiki-code-dev） |
| BR-077 | loadUsers, readFile, JSON.parse, ENOENT, corrupted, backup, .bak | 读取关键文件区分 not-found（默认初始化）与 corrupt（log+备份.corrupt+回退默认），禁静默清零 | references/file-corruption-guard-rule.md（wiki-code-dev） |
| BR-078 | as any, as unknown as, !., InjectOptions, HTTPMethods, request.method, @migration | 禁 as any 绕过类型检查；优先提取真实类型（InjectOptions['method']）/ 类型守卫；迁移期 @migration 标注 | references/type-safe-no-any-rule.md（wiki-code-dev） |
| BR-079 | setTimeout, AbortSignal.timeout, 30000, 30_000, timeout, fetch, 硬编码 | 超时从配置键读取禁硬编码字面量；多层超时自下而上递增 ×1.5（与 BR-053 一致） | references/config-timeout-rule.md（wiki-code-dev） |
| BR-080 | isFfmpegAvailable, ffprobe, ffmpeg -version, capability, 探测, probe | 用 ffmpeg -version 能力检测替代 ffprobe 冗余探针；缺失工具优雅降级 | references/no-redundant-probe-rule.md（wiki-code-dev） |

### ssml-injection（BR-074）
- File: ssml-injection-rule.md
- Rules: 3 (3 critical)
- Core checks: prosody rate/volume/pitch 白名单正则校验 + clamp 默认；用户文本 `<>&` 转义；禁用 `<mstts:express-as>`（免费端点 1007）；前端 FR-073 为防御纵深
- Scope: TTS 端点（/api/tts/synthesize）SSML 构造、prosody 参数来源为用户/前端下发、express-as 使用
- 对应编码规范：CODING-SSML-INJECTION

### child-process-sync（BR-075）
- File: child-process-sync-rule.md
- Rules: 2 (2 critical)
- Core checks: 需同步结果用 execFileSync（带 timeout）或 await Promise，禁异步 execFile 当同步用；子进程调用须超时 + 错误传播
- Scope: audioPostprocess.ts 等 ffmpeg/外部 CLI 封装、需子进程退出码/stdout/产物的同步计算
- 对应编码规范：CODING-CHILD-PROCESS-SYNC

### critical-write-no-swallow（BR-076）
- File: critical-write-no-swallow-rule.md
- Rules: 2 (2 critical)
- Core checks: 关键写（saveUsers/saveConfig/persistConversation）失败须 throw/log+throw，禁空 catch 假成功；返回 ok 须与真实写一致
- Scope: 用户/配置/会话持久化写操作
- 对应编码规范：CODING-CRITICAL-WRITE-NO-SWALLOW

### file-corruption-guard（BR-077）
- File: file-corruption-guard-rule.md
- Rules: 2 (2 critical)
- Core checks: 读取关键 JSON 区分 ENOENT（默认初始化）与 corrupt（log+备份.corrupt+回退默认）；损坏文件须备份而非覆盖
- Scope: users.json/config.json 等关键数据文件读取反序列化
- 对应编码规范：CODING-FILE-CORRUPTION-GUARD

### type-safe-no-any（BR-078）
- File: type-safe-no-any-rule.md
- Rules: 2 (1 critical, 1 suggestion)
- Core checks: 禁 as any 绕过类型检查；优先提取真实类型（InjectOptions['method']）/ 类型守卫；迁移期 @migration 标注放行
- Scope: api/src/**/*.ts 类型强转、request.method 等 Fastify 类型来源
- 对应编码规范：CODING-TYPE-SAFE-NO-ANY（前端 FR-026 / type-safety-rule 泛化上位）

### config-timeout（BR-079）
- File: config-timeout-rule.md
- Rules: 2 (1 critical, 1 suggestion)
- Core checks: 超时从配置键读取禁硬编码字面量（30000 等）；多层超时自下而上递增 ×1.5（与 BR-053 一致）
- Scope: setTimeout/AbortSignal.timeout/execFileSync timeout/fetch 超时等所有超时参数
- 对应编码规范：CODING-CONFIG-TIMEOUT（扩展 BR-034 配置化边界）

### no-redundant-probe（BR-080）
- File: no-redundant-probe-rule.md
- Rules: 2 (1 suggestion, 1 critical)
- Core checks: 用 ffmpeg -version 能力检测替代 ffprobe 冗余探针；缺失工具优雅降级而非崩溃
- Scope: 外部工具（ffmpeg 等）可用性检测逻辑
- 对应编码规范：CODING-NO-REDUNDANT-PROBE

## BR-081 ~ BR-086 新增条目

> v2.7.0 追加的 6 条新规则索引（基于「归档路由重构」四维度复盘提炼），对应 wiki-code-dev 的 CODING-GENERATED-FILENAME-UNIQUENESS / CODING-SAFE-CLIENT-DATE-PARSE / CODING-INTEGER-INDEX-VALIDATION / CODING-WIKILINK-SANITIZATION / CODING-EMPTY-CONTENT-REJECTION / CODING-PERSISTENCE-CLIENT-CONTENT-DECOUPLING（前端门控同步见 FR-076）。
> 格式：规则编号 | 触发关键字 | 核心检查 | 规则文件路径

| 规则编号 | 触发关键字 | 核心检查 | 规则文件路径 |
|---------|-----------|---------|-------------|
| BR-081 | archive, generateFilename, writeFile, date+shortId, collision, 随机后缀, 覆盖, 静默丢数据, 同日同线程 | 落盘文件名若由分组键（日期+shortId）派生非全局唯一，须追加随机后缀（长度配置化）；冲突即重生成/拒绝，禁覆盖（同日同线程多条归档互相覆盖） | references/generated-filename-uniqueness-rule.md（wiki-code-dev） |
| BR-082 | new Date, toISOString, ts, RangeError, 日期解析, 非法日期, 回退默认 | 服务端解析客户端日期串禁 `new Date(str).toISOString()`（非法串抛 RangeError 致 500）；安全解析覆盖 null/未定义/非字符串/无效日期，回退默认 | references/safe-client-date-parse-rule.md（wiki-code-dev） |
| BR-083 | messageIndex, Number.isInteger, 数组访问, 下标, 非整数, 400 | 客户端传下标访问数组前 `Number.isInteger` 校验，非法/缺失即 400；校验在数组访问前短路，禁 undefined 访问 500 | references/integer-index-validation-rule.md（wiki-code-dev） |
| BR-084 | wikilink, [[, refs, replace, trim, 换行, 脏链接, filter(Boolean) | 注入 `[[wikilink]]` 前去换行+trim+去空，按配置正则额外清洗 `]`/`\|`；禁跨行断裂与 `[[ ]]` 脏链接（建议级） | references/wikilink-sanitization-rule.md（wiki-code-dev） |
| BR-085 | question, answer, empty, 空串, 创建型写入, 必填字段, 400, no-op | 创建型写入端点必填字段（question/answer 等）缺失或空串即 400；纯空白视为空，禁空内容 no-op 落盘 | references/empty-content-rejection-rule.md（wiki-code-dev） |
| BR-086 | getSession, threadsPersist, 请求体, 内容解耦, 误报过期, 服务端会话, trust_client_content | 归档内容优先请求体取，仅缺失回退 getSession；threadsPersist=false（默认）部署下依赖服务端会话恒空→100%误报过期，须解耦；前端门控随后端契约同步放宽（与 FR-076 闭环） | references/persistence-client-content-decoupling-rule.md（wiki-code-dev） |
| BR-087 | PUT /api/conversations/:id, body.messages, upsert, merge, append, 全量替换, 幂等, existing.messages, 编辑重发, 悬空答案, removeMessagesFrom | 会话 upsert 须将 `body.messages` 视为权威全量替换（幂等），禁服务端 `existing.messages` merge/append；缺失回退 `[]`；并发/重试以请求体为最终态（前端 edit-resend 后端契约，对应 FR-077 / CODING-EDIT-RESEND） | references/edit-resend-backend-rule.md（wiki-code-dev references/edit-resend-rule.md 同源） |

### generated-filename-uniqueness（BR-081）
- File: generated-filename-uniqueness-rule.md
- Rules: 2 (2 critical)
- Core checks: 落盘文件名由派生分组键（日期+shortId）构成时非全局唯一，须追加随机后缀（collision_suffix_len 配置）；冲突校验禁止覆盖已存在文件
- Scope: 归档/落盘文件名生成、writeFile 写入路径构造、同日同线程多归档
- 对应编码规范：CODING-GENERATED-FILENAME-UNIQUENESS

### safe-client-date-parse（BR-082）
- File: safe-client-date-parse-rule.md
- Rules: 2 (2 critical)
- Core checks: 服务端解析客户端日期串禁 `new Date(str).toISOString()`（非法串 RangeError）；安全解析覆盖 null/未定义/非字符串/无效日期，回退 default_value
- Scope: 归档路由 ts 解析、任何客户端传日期串的服务端解析
- 对应编码规范：CODING-SAFE-CLIENT-DATE-PARSE

### integer-index-validation（BR-083）
- File: integer-index-validation-rule.md
- Rules: 2 (2 critical)
- Core checks: 客户端传下标（messageIndex）访问数组前 Number.isInteger 校验，非法/缺失即 reject_status（400）；校验在数组访问前短路
- Scope: 客户端传索引访问数组的路由、sessionId/messageIndex 解构
- 对应编码规范：CODING-INTEGER-INDEX-VALIDATION

### wikilink-sanitization（BR-084）
- File: wikilink-sanitization-rule.md
- Rules: 2 (2 suggestion)
- Core checks: 注入 `[[wikilink]]` 前 `replace(/[\r\n]/g,' ')` + trim + filter(Boolean) 去空；按 extra_clean_regex 额外清洗 `]`/`\|`
- Scope: refs 注入 wikilink、任何注入 Markdown 链接的字段
- 对应编码规范：CODING-WIKILINK-SANITIZATION

### empty-content-rejection（BR-085）
- File: empty-content-rejection-rule.md
- Rules: 2 (2 critical)
- Core checks: 创建型写入端点必填字段（required_fields）缺失或空串即 reject_status（400）；treat_whitespace_as_empty 纯空白视为空
- Scope: 归档/落盘等创建型写入端点、question/answer 等必填校验
- 对应编码规范：CODING-EMPTY-CONTENT-REJECTION

### persistence-client-content-decoupling（BR-086）
- File: persistence-client-content-decoupling-rule.md
- Rules: 2 (2 critical)
- Core checks: 归档内容优先请求体取，仅缺失回退 getSession；threadsPersist=false（默认）须解耦，禁擅自开 persist；前端门控随后端契约同步放宽（与 FR-076 同一变更闭环）
- Scope: 归档路由内容取源、服务端会话持久化边界、前端 can-archive 门控漂移
- 对应编码规范：CODING-PERSISTENCE-CLIENT-CONTENT-DECOUPLING / CODING-CAPABILITY-GATING-SYNC

### edit-resend-backend-contract（BR-087）
- File: edit-resend-backend-rule.md（wiki-code-dev references/edit-resend-rule.md 为同源规范）
- Rules: 3 (3 critical)
- Core checks: 会话 upsert（PUT /api/conversations/:id）须将 `body.messages` 视为权威全量替换（幂等），禁服务端 `existing.messages` merge/append；upsert 仅保留服务端特有元数据、消息体以请求体为唯一来源、缺失回退 `[]`；并发/重试以请求体 messages 为最终态（最后写覆盖）
- Scope: 会话落盘端点、编辑重发（前端 trim 尾随 AI 答案 + 重插用户消息）后端契约、并发 upsert
- 对应编码规范：CODING-EDIT-RESEND（前端侧 FR-077）

### route-return-completeness（BR-089）
- File: route-return-completeness-backend-rule.md（wiki-code-dev references/route-return-completeness-rule.md 为同源规范）
- Rules: 2 (2 critical)
- Core checks: 路由 handler 每个分支/提前退出点必须显式 return/reply；async handler 禁止漏 return 致 Fastify 隐式 reply.send 双发响应 / 前端超时
- Scope: 所有 Fastify 路由 handler（含多分支校验 / 提前 return reply.send）；preHandler 与 setErrorHandler 接管 throw 的场景除外
- 对应编码规范：CODING-ROUTE-RETURN-COMPLETENESS（扩展 BR-ESM-04 端点可达性）

### response-hook-safe（BR-090）
- File: response-hook-safe-backend-rule.md（wiki-code-dev references/response-hook-safe-rule.md 为同源规范）
- Rules: 2 (1 critical / 1 建议)
- Core checks: 全局 onSend/onResponse/setSerializer/contentTypeParser 必须 try/catch fail-open（异常原样放行）；禁止 await 重计算阻塞全量响应
- Scope: 全局注册的响应生命周期钩子与每响应必执行的插件钩子；路由级局部 onSend 除外
- 对应编码规范：CODING-RESPONSE-HOOK-SAFE（与 BR-091 互补）

### compression-default-off（BR-091）
- File: compression-default-off-backend-rule.md（wiki-code-dev references/compression-default-off-rule.md 为同源规范）
- Rules: 2 (1 critical / 1 major)
- Core checks: 响应压缩中间件默认关闭、仅 config.enable 时条件注册（if 包裹）；阈值/级别/白名单全来自配置，禁硬编码
- Scope: @fastify/compress 等响应体压缩中间件；反向代理传输层压缩与无全局钩子的客户端协商场景除外
- 对应编码规范：CODING-COMPRESSION-DEFAULT-OFF（与 BR-090 互补）

### user-store-init（BR-092）
- File: user-store-init-backend-rule.md（wiki-code-dev references/user-store-init-rule.md 为同源规范）
- Rules: 2 (1 critical / 1 major)
- Core checks: loadUsers/loadConfig 须区分 not-found（→默认）与 corrupt/空壳（→备份+回退默认+log.warn），禁静默清零；初始化写盘须合法非空 JSON
- Scope: 任何"加载即解析 JSON"的关键数据文件（用户库/配置/状态）；纯追加日志与可弃缓存除外
- 对应编码规范：CODING-USER-STORE-INIT（与 BR-076/BR-077 互补）

### server-side-isolation（BR-ISOLATION）
- File: server-side-isolation-rule.md
- Rules: 3 (3 critical)
- Core checks: 写端点须注入 auth 感知守卫 `createIsolationGuards`（auth.enabled=false 单租户直通，禁对单租户引入 401）；带归属资源落盘 `ownerId` 须以 `request.currentUser` 受信上下文盖章、忽略客户端 body、读取不匹配返回 404；限流/审计客户端 IP 须用复合键 `clientIpFromRequest`（`request.ip|xffFirst`）防 XFF 伪造、trustProxy=false
- Scope: 写端点 preHandler 守卫注入、服务端 ownership 盖章、限流/审计 IP 提取、单租户直通形态保持
- 对应编码规范：CODING-ISOLATION（wiki-code-dev）