{
  "_meta": {
    "purpose": "最小 Python 项目示例（仅启用通用 core 硬约束）",
    "version": "2.0.0",
    "suitable_for": "纯 Python 项目（无 FastAPI/Django 等特定框架）"
  },
  "project": {
    "key": "my_python_project",
    "name": "My Python Project",
    "base_path": "../..",
    "description": "简单 Python 项目示例"
  },
  "modules": {
    "backend": {
      "path": "src",
      "languages": ["python"],
      "frameworks": [],
      "layers": {
        "core": "core",
        "utils": "utils",
        "models": "models"
      },
      "exclude": ["__pycache__", ".git", "dist", "build", "tests", "venv", ".venv"]
    }
  },
  "verify": {
    "test_command": "pytest tests/"
  },
  "report": {
    "output_dir": "docs/sonar-reports/"
  },
  "framework_extensions": {},
  "hard_constraints": {
    "_note": "此示例仅启用 core.json（通用安全规则），不启用任何业务规则",
    "enabled_business_rules": []
  },
  "_usage_notes": [
    "1. 复制此文件到 ../../config/project_config.json",
    "2. 修改 project.key 为你的项目名",
    "3. 根据需要调整 modules.backend.path",
    "4. 设置环境变量 SONAR_TOKEN 和 SONARQUBE_URL",
    "5. 运行 .\\scripts\\verify-connection.ps1 验证",
    "6. 首次扫描会加载所有 core 硬约束规则（14 条通用安全规则）"
  ]
}
