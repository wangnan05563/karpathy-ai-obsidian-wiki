---
title: 恒生电子数据库编码规范
type: concept
created: 2025-01-21
updated: 2025-01-21
source: 恒生电子股份有限公司内部文档《数据库编码规范》（2021年07月）
tags: [数据库, 编码规范, SQL, MySQL, Oracle, 恒生电子]
---

# 恒生电子数据库编码规范

## 概述

本文档由**恒生电子股份有限公司** EPG 研发组编制（批准日期：2020/12/17），旨在统一数据库设计与 SQL 代码编写风格，使代码更易于阅读、理解和继承。

## 约束等级定义

| 约束等级 | 约束效力 | 强制性 |
|---------|---------|-------|
| 【强制】 | 违反该项将被认为代码存在严重缺陷 | 团队必须遵守 |
| 【推荐】 | 违反该项将被认为代码存在轻微缺陷 | 根据具体产品特性的不同，选择性地遵守 |
| 【参考】 | 违反该项可被认为代码存在优化空间 | 从产品持续优化及人员技能提升的角度，参考使用 |

## 命名规范

### 对象命名

**【推荐】** 对象的命名由三部分组成：**对象前缀 + 模块名 + 对象标识**，使用下划线 `_` 进行分隔，长度不超过 30 个字符。

示例：`v_crm_custinfo`

#### 对象前缀对照表

| 对象类型 | 对象前缀 | 说明 |
|---------|---------|------|
| 表（table） | 无 | — |
| 临时表（temporary table） | `t` 或 `tmp` | — |
| 视图（view） | `v` | 格式：`v_表名`（不建议使用视图） |
| 非唯一索引（index） | `idx` | 格式：`idx_表名_每列首字母` |
| 唯一索引（unique index） | `uk` | 格式：`uk_表名_每列首字母`（若索引字段只有一个，建议为 `uk_表名_列名`） |
| 主键（primary key） | `pk` | 格式：`pk_表名` |
| 过程（procedure） | `p` 或 `sp` | 不建议使用 |
| 函数（function） | `f` 或 `fn` | 不建议使用 |

**建议检查阶段**：设计评审、代码提交  
**检查方式**：扫描工具

**【推荐】** 命名只能使用英文字母、数字和下划线，全部使用小写字符。禁止以数字开头，名称前后不能加引号。

### 变量和参数命名

*（详细规范参见原始文档第 5 页及后续章节）*

## 格式规范

涵盖缩进、大小写、换行、空格、注释等编码格式要求。

## 设计规范

### 通用部分
适用于所有数据库的通用设计原则。

### MySQL 特有
针对 [[MySQL]] 数据库的特定设计规范。

### Oracle 特有
针对 [[Oracle]] 数据库的特定设计规范。

## 语法规范

### 通用部分
适用于所有数据库的通用 SQL 语法规则。

### MySQL 特有
[[MySQL]] 特有的语法要求。

### Oracle 特有
[[Oracle]] 特有的语法要求。

## 版本移植

涉及数据库版本升级或跨版本移植时的注意事项。

## 脚本规范

### 脚本规范总则

#### 通用部分
#### MySQL 特有
#### Oracle 特有

### 数据库对象

#### 通用部分

### 表数据

#### 通用部分
#### MySQL 特有
#### Oracle 特有

## 附录

包含 [[MySQL]] 和 [[Oracle]] 的完整脚本示例。

## 文档修改记录

原始文档包含完整的版本修改追踪记录。

---

## 相关页面

- [[MySQL]]
- [[Oracle]]
- [[sql-coding-best-practices]]
