# SonarQube 修复报告 - karpathy-wiki

**扫描日期**: 2026-08-17 22:30  
**项目**: karpathy_wiki_api (API 后端) / karpathy_wiki_web (前端)  
**SonarQube 版本**: 26.1.0.118079  
**扫描器**: sonar-scanner 8.0.1.6346  

---

## 质量门禁

### karpathy_wiki_api

| 指标 | 实际值 | 阈值 | 状态 |
|------|--------|------|------|
| 新代码覆盖率 | 0.0% | ≥ 80% | ❌ ERROR |
| 新重复代码密度 | 1.92% | ≤ 3% | ✅ OK |
| 新安全热点审查率 | 0.0% | 100% | ❌ ERROR |
| 新违规数 | 0 | = 0 | ✅ OK |

**说明**: 质量门禁 ERROR 是因为测试覆盖率（0.0%）和安全热点审查（0.0%）未达标，这是过程/测试覆盖问题，非代码缺陷。

---

## 问题统计

### karpathy_wiki_api — 27 → 0 ✅（全部清零）

| 严重级别 | 修复前 | 修复后 |
|----------|--------|--------|
| CRITICAL | 7 | 0 |
| MAJOR | 11 | 0 |
| MINOR | 9 | 0 |
| **总计** | **27** | **0** |

### karpathy_wiki_web — 194 个未修复（前端项目）

| 严重级别 | 数量 |
|----------|------|
| BLOCKER | 1 |
| CRITICAL | ~15 |
| MAJOR | ~80 |
| MINOR | ~98 |

---

## 修复详情 — karpathy_wiki_api

### 1. 认知复杂度 (S3776) — 3 个 CRITICAL

| 文件 | 函数 | 修复方式 |
|------|------|----------|
| `src/data-clean/quality-scanner.ts:42` | `scanVaultRaw` | 提取 `processScanFile` 辅助函数 |
| `src/routes/url-ingest.ts:65` | 处理逻辑 | 提取 `processCrawlEvents` 辅助函数 |
| `src/utils/url-crawl.ts:182,376` | SSRF 校验 | 提取 `isRestrictedIpv4/6`、`processProbeResponse/Error` |

### 2. void 操作符 (S3735) — 2 个 CRITICAL

| 文件 | 行号 | 修复方式 |
|------|------|----------|
| `src/routes/query.ts` | 128, 217 | `void reply.send()` → `await reply.send()` |

### 3. 排序比较函数 (S2871) — 1 个 CRITICAL

| 文件 | 行号 | 修复方式 |
|------|------|----------|
| `src/data-clean/dedup-engine.ts` | 158 | `parts.sort()` → `parts.sort((a, b) => a.localeCompare(...))` |

### 4. 其他问题 (MAJOR + MINOR)

| 规则 | 文件 | 修复方式 |
|------|------|----------|
| S4138 | `dedup-engine.ts:170` | `for` → `for-of` |
| S1128 | `index.ts:7,13` | 移除未使用的导入 |
| S7785 | `index.ts:540` | promise chain → top-level await |
| S6324 | `files.ts:60` | NOSONAR（故意匹配控制字符用于文件名净化） |
| S3358 | `url-ingest.ts:113` | 提取嵌套三元运算 |
| S7718 | `url-ingest.ts:121`, `url-crawl.ts:389` | 重命名 catch 参数 |
| S4325 | `url-ingest.ts:144` | 移除无用 `as` 断言 |
| S5850 | `url-crawl.ts:183,213` | 正则表达式添加非捕获组 |
| S6582 | `url-crawl.ts:348` | 使用可选链 `?.` |
| S4822 | `url-crawl.ts:430,790` | 使用 `await` 处理 Promise |
| S107 | `query-workflow.ts:576` | 参数对象化 |
| S2933 | `stream-run-manager.ts:79` | 添加 `readonly` |
| S1854 | `stream-run-manager.ts:105` | 移除无用赋值 |
| S7763 | `threads.ts:224` | 使用 `export…from` |

---

## 测试验证

```
测试文件: 32 passed (32)
测试用例: 457 passed (457)
编译检查: tsc --noEmit → exit code 0 ✅
```

---

## 待处理项

1. **Web 项目 (karpathy_wiki_web)**: 194 个开放问题，主要为 CSS 可访问性、TypeScript 编码风格、Vue 模板可访问性，建议分批修复
2. **测试覆盖率**: 当前 0.0%，目标 80%，需补充单元测试
3. **安全热点审查**: 当前 0%，目标 100%，需手动审查