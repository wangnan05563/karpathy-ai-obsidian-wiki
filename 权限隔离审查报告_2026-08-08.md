# 系统权限隔离逻辑全面排查报告

> 排查日期：2026-08-08
> 依据规则：`wiki-code-dev` 的隔离相关规则（frontend-session-isolation / byok-per-user-override / auth-endpoint-classification / session-store / api-key-resolution / sensitive-field-masking / rate-limit-resilience / runtime-data-privacy / storage-boundary）
> 范围：`karpathy-wiki` 后端（`api/src`）+ 前端（`frontend/src`）

---

## 一、整体结论

本项目的隔离策略呈现**「客户端强隔离 / 服务端弱（缺）授权」**的明显分层：

- **客户端（前端）隔离设计良好**：`filterByOwner` 严格按 `ownerId` 隔离、`migrateOwnerless` 落盘后才内存归属、`resetSession` 已接入 auth watch、`scopedOwnerId` 已返回、`persistConversation` 复用 id 前以存储实际记录校验归属；BYOK 密钥仅经请求体下发、不落盘、缺密钥 400 不回落；敏感字段已脱敏。
- **服务端授权边界缺失是主要矛盾**：全局 `preHandler` 只**注入** `currentUser` 而**不拒绝**未认证请求；除 `/api/auth/*` 外的路由（会话、线程记忆、共享配置写接口等）没有任何 `requireAuth`/`requireAdmin` 守卫，且数据以客户端提供的 UUID/`threadId` 为唯一键，**无 owner 归属绑定**。只要能到达服务端口，即可越权读写/删除他人数据与共享配置。

> 关键矛盾：项目已具备完整的登录/注册/用户管理（admin）认证基础设施，说明有多用户意图，但数据面与配置面几乎完全未受保护。

---

## 二、各模块逐项审查

### 模块 1：认证中间件 / 端点分类（对应 `auth-endpoint-classification`）
**文件**：`api/src/middleware/auth.ts`、`api/src/routes/auth.ts`

| 项 | 现状 |
|---|---|
| 全局 `preHandler` | 仅解析 token 并注入 `request.currentUser`；公开路径/无 token/无效 token 均**直接 return，不发 401**（auth.ts:55-78） |
| 守卫分布 | `requireAuth`/`requireAdmin` 仅挂在 `/api/auth/*`（`auth.ts` 内 login/register/me/logout/permissions/users/audit），其余路由零守卫 |
| `auth.enabled=false` | 全局视为 admin，跳过一切认证（auth.ts:46-50） |
| `publicPaths` 来源 | `DEFAULT_PUBLIC_PATHS` 为**硬编码常量**（auth.ts:36-40），违反该规则自身「禁止硬编码端点路径、须从 config 读取」的要求 |

**问题**
- ①（high）默认「游客可访问一切非 `/api/auth` 路由」——浏览类接口可接受，但**写接口（会话/配置）同样无门槛**。
- ②（medium）`DEFAULT_PUBLIC_PATHS` 硬编码，违反 `auth-endpoint-classification` 规则中「参数从 config 读取」的硬约束；且 `isPublicPath` 用 `startsWith(p + '/')`，`/health` 会误匹配 `/health/xxx`。

**优化建议**
- 改为「默认拒绝 + 显式公开清单」：全局 `preHandler` 对未登录且非白名单路由直接 401（或将白名单收敛到真正公开的少数端点）。
- `publicPaths` 抽到 `config` 单一来源（保留 `DEFAULT_PUBLIC_PATHS` 作为兜底常量，但由 config 覆盖），并将 `isPublicPath` 改为精确匹配或严格前缀。

---

### 模块 2：会话后端持久化隔离（对应 `frontend-session-isolation` 的服务端镜像）
**文件**：`api/src/routes/conversations.ts`

| 项 | 现状 |
|---|---|
| `ConversationRecord` | **无 `ownerId` 字段**（conversations.ts:22-33） |
| 路由守卫 | 全部无 `preHandler` 守卫 |
| `GET /api/conversations` | 列出**所有**会话摘要，无归属过滤（68-102） |
| `GET/PUT/DELETE /:id` | 仅按 UUID 操作，无 owner 校验（105-178） |
| 路径穿越 | UUID 正则已防穿越（✓） |

**问题（CRITICAL）**
任意可达客户端可通过 UUID 枚举**读/覆盖/删除**任意用户的会话历史，且完全无认证。即便前端已迁 IndexedDB（FR-RM-05）不再调用此路由，该遗留路由仍被注册暴露，是真实的安全洞。

**优化建议**
- 若保留：加 `requireAuth` + 在记录中引入 `ownerId`，所有读写校验 `record.ownerId === currentUser.userId`（沿用前端 `dbGet` 校验归属的同一思路，改为服务端落地）。
- 若不保留：从 `index.ts` 注销该路由，避免遗留暴露面。

---

### 模块 3：线程记忆隔离（对应 `session-store` 引用模式 + owner 绑定）
**文件**：`api/src/routes/query.ts`、`api/src/engine/thread-memory-store.ts`、`api/src/routes/threads.ts`

| 项 | 现状 |
|---|---|
| `threadId` 来源 | 客户端请求体提供（query.ts:34, 118-126），仅做 UUID 格式校验 |
| 存储索引 | `ThreadMemoryStore` 仅按 `threadId` 索引，**无 `userId` 列** |
| `getHistoryContext(threadId)` | 按 threadId 直接取历史上下文注入 LLM（query.ts:128） |
| `/api/query/archive` | 按 `threadId + messageIndex` 从 `store.getSession` 取内容返回（无 owner 校验） |
| nonce/防篡改 | `trust_client_content=false` 思路未被采用（仍信任前端提供 threadId） |

**问题（CRITICAL）**
只要知道他人的 `threadId`（UUID），即可**读取并续写**其问答记忆——跨用户隐私泄漏，且泄漏面随会话数增长。这是与模块 2 同构的服务端「无 owner 绑定」缺陷。

**优化建议**
- 线程创建时绑定 `ownerId = currentUser?.userId ?? 'guest-<ip>'`，`getHistoryContext`/`getSession`/`ensureThread` 必须校验 thread 归属，越界返回 404。
- 或：线程存储以 `userId` 派生密钥加密，即使 threadId 泄露也无法解密他人内容。
- 注意：当前 `query` 允许游客（无 token）访问，若强制绑定 owner，需先解决模块 1 的游客策略。

---

### 模块 4：BYOK 密钥隔离（对应 `byok-per-user-override`）
**文件**：`api/src/engine/byok-override.ts`、`api/src/routes/query.ts`、`frontend/src/services/userConfig.ts`

| 项 | 现状 | 评价 |
|---|---|---|
| 密钥落盘 | 后端仅本次请求内存使用，不写盘、不回显 GET、不记日志 | ✓ 合规 |
| 缺密钥回落 | `query.ts:71-81` 缺 apiKey/provider/baseUrl/model 直接 400，不回落服务端共享 | ✓ 合规 |
| 覆盖纯函数 | `applyPerRequestOverride` 为纯函数 | ✓ 合规 |
| 空 tools 回退 | 空 `toolsConfig` 视为「未提供」，回退服务端共享 MCP/CLI（byok-override.ts） | ✓ 合理 |
| 前端命名空间 | `usercfg::<kind>::<userId>` 按用户隔离 | ✓ 合规 |

**问题**：无实现缺陷。仅 1 项**待确认**——未配置工具的用户会继承服务端**共享工具能力**（能力泄漏而非密钥泄漏），需确认是否符合「各用户能力严格隔离」的预期；若要求严格隔离，空 tools 应回退为「无工具」而非「服务端共享工具」。

---

### 模块 5：敏感字段脱敏（对应 `sensitive-field-masking`）
**文件**：`api/src/routes/ai.ts`、`api/src/routes/config.ts`、`api/src/config.ts`

| 项 | 现状 | 评价 |
|---|---|---|
| GET 脱敏 | `apiKeyMasked` + `configured` 标志（ai.ts:61-78, 299-315, config.ts:23-50） | ✓ 合规 |
| POST 空串语义 | `****` 前缀=不修改、空串=清空（ai.ts:113-121） | ✓ 合规 |
| 日志 | 请求日志仅 method/url/status/elapsed，不含请求体，apiKey 不落日志 | ✓ 合规 |

**问题**：脱敏本身良好，但**这些 GET/PUT 接口无 auth 守卫**（见模块 6），游客可读 `apiKeySet`/`apiKeyRef`/`vaultPath`/`server` 等配置元信息。

---

### 模块 6：共享服务端配置写接口（对应 `auth-endpoint-classification` + `byok`）
**文件**：`api/src/routes/ai.ts`、`api/src/routes/config.ts`

| 接口 | 守卫 | 风险 |
|---|---|---|
| `PUT /api/ai/config`（ai.ts:94） | **无** | 游客可持久化共享服务端 apiKey/baseUrl/model/provider 到 config.json —— 破坏 BYOK 意图、可劫持全部用户流量 |
| `PUT /api/ai/web-search`（ai.ts:320） | **无** | 同上，可写共享搜索密钥 |
| `POST /api/ai/reset-config`（ai.ts:193） | **无** | 游客可清空 AI 配置 |
| `POST /api/ai/test-connection`（ai.ts:226） | **无** | 游客可探测任意 provider（SSRF 面） |
| `PUT /api/config/budget|health-check|batch|logging`（config.ts） | **无** | 游客可改全局服务配置 |

**问题（CRITICAL）**：全部写接口无任何 `requireAuth`/`requireAdmin`，与「用户管理（admin）」权限体系直接矛盾，构成越权配置篡改。

**优化建议**：统一加 `requireAdmin`（与 `users` 管理同权限），并与 `auth-endpoint-classification` 的 config 化清单联动做 CI 静态校验（新增写接口默认必须带守卫）。

---

### 模块 7：限流 / 真实 IP（对应 `rate-limit-resilience`）
**文件**：`api/src/auth/auth-rate-limit.ts`

| 项 | 现状 |
|---|---|
| 真实 IP | `clientIpFromRequest` 优先取 `X-Forwarded-For` **首跳**（auth-rate-limit.ts:24-31），缺失才回退 `request.ip` |
| 双维度 | 单 IP 10/min、单用户名 5/min，固定窗口 + 惰性 sweep（✓ 有界、✓ 不跨窗口累积） |
| 全局限流 | `@fastify/rate-limit` 已注册（60/min；GET 300/min；query 20/min） |

**问题（medium）**：`X-Forwarded-For` 首跳**完全由客户端可控**。攻击者可每次请求轮换 `X-Forwarded-For` 头，使 per-IP 桶始终为「新桶」，从而**绕过 10/min 登录/注册暴力破解限流**。注释（auth-rate-limit.ts:22）声称「避免 XFF 伪造导致绕过」，但实现恰恰优先采信 XFF 首跳，自相矛盾。

**优化建议**
- 直连场景一律用 `request.ip`；仅当在 `config` 中**显式声明可信代理段**（trustProxy 明确配置）时才采信 XFF，且取**最后一个可信跳**。
- 或：以 `hash(request.ip + xff首跳)` 作为桶 key，单一字段伪造不足以刷新整桶。

---

### 模块 8：运行时数据隐私 / .gitignore（对应 `runtime-data-privacy`）
**文件**：`karpathy-wiki/.gitignore`

| 目录 | 状态 |
|---|---|
| `data/conversations/` | ✓ 已忽略（line 55） |
| `data/threads/`（含 session.json/memory.json 敏感问答） | ✓ 已忽略（line 57） |
| `data/vault/`（raw/entities/queries 等） | ✓ 已忽略（line 48-53） |

**结论**：合规，规则要求的「新增数据目录随兄弟目录一起 gitignore」「会话默认不在服务端存储」均已满足（`data/threads` 已正确忽略，未遗漏 sibling）。

---

### 模块 9：前端会话隔离（对应 `frontend-session-isolation`）—— 良好
**文件**：`frontend/src/stores/conversations.ts`、`frontend/src/views/Query.vue`

| 检查点 | 现状 | 评价 |
|---|---|---|
| 会话状态 ref 全部 return | `currentConversationId`/`scopedOwnerId`/`conversations` 均在 return（conversations.ts:130,158-162） | ✓ 合规 |
| auth watch 先 reset 再 load | `Query.vue:136-140` 监听 `user?.id` 变化先 `resetSession()` | ✓ 合规 |
| 复用 id 前校验归属 | `persistConversation` 以 `dbGet` 校验 owner，冲突改用新 uuid（174-231） | ✓ 合规 |
| `filterByOwner` 严格 | 已登录 `owner===owner`，未登录返回空（47-52） | ✓ 合规 |
| `migrateOwnerless` 落盘后内存归属 | 先 `dbPut` 成功再内存 claim（59-74） | ✓ 合规 |

**优化建议（low）**：补充「真实登出/切换账户」的跨账户 E2E 回归（非 store 直接 `setUser`），将此客户端隔离固化为 CI 门禁，防止回退。该模块本身无需修复。

---

## 三、问题清单汇总（按严重级别）

| 级别 | 模块 | 问题 | 文件:行 |
|---|---|---|---|
| **CRITICAL** | 2 | 会话路由无 owner、无 guard，UUID 可枚举越权读写删 | conversations.ts:22,68,105,119,157 |
| **CRITICAL** | 3 | 线程记忆仅按客户端 threadId 索引，无 owner 绑定，跨用户泄漏 | query.ts:118-128；thread-memory-store.ts |
| **CRITICAL** | 6 | 共享配置写接口（ai/config、web-search、reset-config、test-connection、config/*）无 guard，游客可篡改/清空共享密钥 | ai.ts:94,193,226,320；config.ts |
| **HIGH** | 1 | 全局 preHandler 只注入不拒绝，非 `/api/auth` 路由默认游客可写 | auth.ts:55-78 |
| **MEDIUM** | 1 | `publicPaths` 硬编码，违反自身「config 化」规则；`startsWith` 误匹配 | auth.ts:36-40,82-86 |
| **MEDIUM** | 7 | 限流优先采信 `X-Forwarded-For` 首跳，可伪造绕过暴力破解限流 | auth-rate-limit.ts:24-31 |
| **LOW** | 4 | 空 tools 回退服务端共享能力，待确认是否符合严格隔离预期 | byok-override.ts |
| **LOW** | 9 | 缺跨账户 E2E 回归门禁 | （测试侧） |
| ✓ 合规 | 5 | 敏感字段脱敏/空串语义/日志均正确 | ai.ts/config.ts |
| ✓ 合规 | 8 | 运行时数据目录均已 gitignore | .gitignore |
| ✓ 合规 | 4/9 | BYOK 不落盘+400、前端会话隔离实现良好 | — |

---

## 四、整改优先级建议

- **P0（必须修）**：模块 2/3/6 服务端授权缺失 → 给数据面（会话、线程记忆）加 `ownerId` 绑定 + `requireAuth`；给配置写面（ai/config、web-search、reset-config、test-connection、config/*）加 `requireAdmin`。这是越权读/写/删他人数据与共享密钥的根因。
- **P1（应修）**：模块 1 将 `publicPaths` config 化 + 全局默认拒绝（仅白名单公开），消灭「游客可写」的默认姿态。
- **P2（建议修）**：模块 7 限流改为直连用 `request.ip`、显式可信代理才采信 XFF，堵住暴力破解绕过的口子。
- **P3（确认/加固）**：模块 4 确认空 tools 是否应回退为「无工具」；模块 9 补跨账户 E2E。

---

## 五、可沉淀的规则（建议回写 `wiki-code-dev`）

1. **新增后端数据隔离规则（建议 BR-ISOLATION-01, critical）**：任何以用户身份隔离的**服务端**数据 API（会话/记忆/文件/线程），必须同时满足：① 路由带 `requireAuth`；② 记录含 `ownerId` 且所有读写校验 `owner === currentUser.userId`；③ 客户端提供的 id（UUID/threadId）仅作命名空间，**不可作为归属依据**。复用前端 `frontend-session-isolation` 的「以存储实际记录校验归属」思路下沉到服务端。
2. **新增写接口默认拒绝规则（建议 BR-ISOLATION-02, critical）**：所有 `POST/PUT/DELETE` 配置/管理接口必须显式带 `requireAuth`/`requireAdmin`；CI 静态扫描「新增写路由无守卫」即报错（与 `auth-endpoint-classification` 联动）。
3. **限流 IP 来源规则补充（rate-limit-resilience R-1 修订）**：明确「`X-Forwarded-For` 仅在显式可信代理下采信，否则用 socket IP；或组合哈希桶」，消除「优先首跳」的伪造绕过。
4. **`auth-endpoint-classification` 自检**：当前 `DEFAULT_PUBLIC_PATHS` 硬编码违反其自身「禁止硬编码端点路径」约束，需 config 化并补精确匹配。
